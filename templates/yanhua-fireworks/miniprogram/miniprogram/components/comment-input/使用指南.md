# Comment Input 组件使用指南

## 📖 目录

1. [如何发表评论](#如何发表评论)
2. [如何回复评论](#如何回复评论)
3. [完整集成示例](#完整集成示例)
4. [常见问题](#常见问题)

---

## 如何发表评论

### 第一步：在页面中引入组件

在页面的 `.json` 文件中引入组件：

```json
{
  "usingComponents": {
    "comment-input": "/components/comment-input/comment-input"
  }
}
```

### 第二步：在页面中添加组件

在页面的 `.wxml` 文件中添加组件：

```xml
<!-- 发表评论按钮 -->
<button bindtap="showCommentInput">发表评论</button>

<!-- 评论输入组件 -->
<comment-input
  id="commentInput"
  videoId="{{videoId}}"
  show="{{showCommentInput}}"
  autoFocus="{{true}}"
  bind:submit="onCommentSubmit"
  bind:cancel="onCommentCancel"
/>
```

### 第三步：在页面逻辑中处理事件

在页面的 `.ts` 文件中添加逻辑：

```typescript
Page({
  data: {
    videoId: 1,                    // 视频ID
    showCommentInput: false        // 是否显示评论输入框
  },

  /**
   * 显示评论输入框
   */
  showCommentInput() {
    this.setData({
      showCommentInput: true
    });
  },

  /**
   * 提交评论
   */
  onCommentSubmit(e: any) {
    const { videoId, content } = e.detail;
    
    console.log('提交评论:', { videoId, content });
    
    // 调用后端API发布评论
    wx.request({
      url: 'https://your-api.com/api/interaction/comment',
      method: 'POST',
      data: {
        videoId: videoId,
        content: content
      },
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res: any) => {
        if (res.data.code === 200) {
          wx.showToast({
            title: '评论成功',
            icon: 'success',
            duration: 2000
          });
          
          // 重置输入框
          const commentInput = this.selectComponent('#commentInput') as any;
          commentInput.reset();
          
          // 隐藏输入框
          this.setData({
            showCommentInput: false
          });
          
          // 刷新评论列表
          this.loadComments();
        } else {
          wx.showToast({
            title: res.data.message || '评论失败',
            icon: 'none',
            duration: 2000
          });
          
          // 重置提交状态
          const commentInput = this.selectComponent('#commentInput') as any;
          commentInput.setSubmitting(false);
        }
      },
      fail: () => {
        wx.showToast({
          title: '网络错误',
          icon: 'none',
          duration: 2000
        });
        
        // 重置提交状态
        const commentInput = this.selectComponent('#commentInput') as any;
        commentInput.setSubmitting(false);
      }
    });
  },

  /**
   * 取消评论
   */
  onCommentCancel() {
    this.setData({
      showCommentInput: false
    });
  },

  /**
   * 加载评论列表
   */
  loadComments() {
    // 重新加载评论列表的逻辑
    console.log('刷新评论列表');
  }
});
```

### 使用流程

1. **用户点击"发表评论"按钮** → 调用 `showCommentInput()` 方法
2. **显示评论输入框** → `showCommentInput` 设置为 `true`
3. **用户输入评论内容** → 组件内部处理输入和字数统计
4. **用户点击"发送"按钮** → 触发 `submit` 事件
5. **页面处理提交** → 调用 `onCommentSubmit()` 方法
6. **调用后端API** → 发送评论到服务器
7. **提交成功** → 调用 `reset()` 重置输入框，隐藏组件，刷新列表
8. **提交失败** → 调用 `setSubmitting(false)` 恢复状态

---

## 如何回复评论

### 第一步：在页面中添加回复组件

在页面的 `.wxml` 文件中添加回复组件：

```xml
<!-- 评论列表组件 -->
<comment-list
  videoId="{{videoId}}"
  comments="{{comments}}"
  bind:reply="onReply"
/>

<!-- 回复输入组件 -->
<comment-input
  id="replyInput"
  videoId="{{videoId}}"
  parentId="{{replyComment.id}}"
  replyToId="{{replyComment.userId}}"
  replyToName="{{replyComment.userName}}"
  show="{{showReplyInput}}"
  autoFocus="{{true}}"
  bind:submit="onReplySubmit"
  bind:cancel="onReplyCancel"
/>
```

### 第二步：在页面逻辑中处理回复

在页面的 `.ts` 文件中添加回复逻辑：

```typescript
Page({
  data: {
    videoId: 1,
    comments: [],                  // 评论列表
    showReplyInput: false,         // 是否显示回复输入框
    replyComment: null             // 要回复的评论对象
  },

  /**
   * 点击回复按钮（由 comment-list 组件触发）
   */
  onReply(e: any) {
    const { comment } = e.detail;
    
    console.log('回复评论:', comment);
    
    this.setData({
      showReplyInput: true,
      replyComment: comment
    });
  },

  /**
   * 提交回复
   */
  onReplySubmit(e: any) {
    const { videoId, content, parentId, replyToId } = e.detail;
    
    console.log('提交回复:', { videoId, content, parentId, replyToId });
    
    // 调用后端API发布回复
    wx.request({
      url: 'https://your-api.com/api/interaction/comment',
      method: 'POST',
      data: {
        videoId: videoId,
        content: content,
        parentId: parentId,      // 父评论ID
        replyToId: replyToId     // 回复目标用户ID
      },
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res: any) => {
        if (res.data.code === 200) {
          wx.showToast({
            title: '回复成功',
            icon: 'success',
            duration: 2000
          });
          
          // 重置输入框
          const replyInput = this.selectComponent('#replyInput') as any;
          replyInput.reset();
          
          // 隐藏输入框
          this.setData({
            showReplyInput: false,
            replyComment: null
          });
          
          // 刷新评论列表
          this.loadComments();
        } else {
          wx.showToast({
            title: res.data.message || '回复失败',
            icon: 'none',
            duration: 2000
          });
          
          // 重置提交状态
          const replyInput = this.selectComponent('#replyInput') as any;
          replyInput.setSubmitting(false);
        }
      },
      fail: () => {
        wx.showToast({
          title: '网络错误',
          icon: 'none',
          duration: 2000
        });
        
        // 重置提交状态
        const replyInput = this.selectComponent('#replyInput') as any;
        replyInput.setSubmitting(false);
      }
    });
  },

  /**
   * 取消回复
   */
  onReplyCancel() {
    this.setData({
      showReplyInput: false,
      replyComment: null
    });
  },

  /**
   * 加载评论列表
   */
  loadComments() {
    // 重新加载评论列表的逻辑
    console.log('刷新评论列表');
  }
});
```

### 回复流程

1. **用户点击评论的"回复"按钮** → comment-list 组件触发 `reply` 事件
2. **页面接收回复事件** → 调用 `onReply()` 方法
3. **显示回复输入框** → 设置 `showReplyInput` 为 `true`，保存 `replyComment` 对象
4. **输入框显示 @用户名** → 组件自动显示"回复 @用户名"
5. **用户输入回复内容** → 组件内部处理输入
6. **用户点击"发送"按钮** → 触发 `submit` 事件
7. **页面处理提交** → 调用 `onReplySubmit()` 方法
8. **调用后端API** → 发送回复到服务器（包含 parentId 和 replyToId）
9. **提交成功** → 重置输入框，隐藏组件，刷新列表

---

## 完整集成示例

### 页面配置 (page.json)

```json
{
  "navigationBarTitleText": "视频详情",
  "usingComponents": {
    "comment-list": "/components/comment-list/comment-list",
    "comment-input": "/components/comment-input/comment-input"
  }
}
```

### 页面模板 (page.wxml)

```xml
<view class="page">
  <!-- 视频播放区域 -->
  <view class="video-section">
    <!-- 视频播放器 -->
  </view>

  <!-- 评论区域 -->
  <view class="comment-section">
    <view class="comment-header">
      <text class="comment-title">评论 ({{comments.length}})</text>
      <button class="comment-btn" bindtap="showCommentInput">发表评论</button>
    </view>

    <!-- 评论列表 -->
    <comment-list
      videoId="{{videoId}}"
      comments="{{comments}}"
      hasMore="{{hasMoreComments}}"
      loading="{{loadingComments}}"
      showEmpty="{{comments.length === 0}}"
      bind:reply="onReply"
      bind:loadMore="onLoadMoreComments"
    />
  </view>

  <!-- 发表评论输入框 -->
  <comment-input
    id="commentInput"
    videoId="{{videoId}}"
    show="{{showCommentInput}}"
    autoFocus="{{true}}"
    bind:submit="onCommentSubmit"
    bind:cancel="onCommentCancel"
  />

  <!-- 回复评论输入框 -->
  <comment-input
    id="replyInput"
    videoId="{{videoId}}"
    parentId="{{replyComment.id}}"
    replyToId="{{replyComment.userId}}"
    replyToName="{{replyComment.userName}}"
    show="{{showReplyInput}}"
    autoFocus="{{true}}"
    bind:submit="onReplySubmit"
    bind:cancel="onReplyCancel"
  />
</view>
```

### 页面逻辑 (page.ts)

```typescript
Page({
  data: {
    videoId: 1,
    comments: [],
    hasMoreComments: false,
    loadingComments: false,
    commentPage: 1,
    commentPageSize: 10,
    
    showCommentInput: false,
    showReplyInput: false,
    replyComment: null
  },

  onLoad(options: any) {
    const videoId = parseInt(options.id || '1');
    this.setData({ videoId });
    this.loadComments();
  },

  /**
   * 加载评论列表
   */
  loadComments() {
    this.setData({ loadingComments: true });

    wx.request({
      url: 'https://your-api.com/api/interaction/comment/list',
      method: 'GET',
      data: {
        videoId: this.data.videoId,
        page: 1,
        pageSize: this.data.commentPageSize
      },
      success: (res: any) => {
        if (res.data.code === 200) {
          this.setData({
            comments: res.data.data.records,
            hasMoreComments: res.data.data.records.length >= this.data.commentPageSize,
            commentPage: 1,
            loadingComments: false
          });
        }
      },
      fail: () => {
        this.setData({ loadingComments: false });
        wx.showToast({
          title: '加载失败',
          icon: 'none'
        });
      }
    });
  },

  /**
   * 加载更多评论
   */
  onLoadMoreComments() {
    if (this.data.loadingComments || !this.data.hasMoreComments) {
      return;
    }

    const nextPage = this.data.commentPage + 1;
    this.setData({ loadingComments: true });

    wx.request({
      url: 'https://your-api.com/api/interaction/comment/list',
      method: 'GET',
      data: {
        videoId: this.data.videoId,
        page: nextPage,
        pageSize: this.data.commentPageSize
      },
      success: (res: any) => {
        if (res.data.code === 200) {
          const newComments = res.data.data.records;
          this.setData({
            comments: [...this.data.comments, ...newComments],
            hasMoreComments: newComments.length >= this.data.commentPageSize,
            commentPage: nextPage,
            loadingComments: false
          });
        }
      },
      fail: () => {
        this.setData({ loadingComments: false });
      }
    });
  },

  /**
   * 显示评论输入框
   */
  showCommentInput() {
    this.setData({ showCommentInput: true });
  },

  /**
   * 提交评论
   */
  onCommentSubmit(e: any) {
    const { videoId, content } = e.detail;

    wx.request({
      url: 'https://your-api.com/api/interaction/comment',
      method: 'POST',
      data: { videoId, content },
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res: any) => {
        if (res.data.code === 200) {
          wx.showToast({ title: '评论成功', icon: 'success' });
          
          const commentInput = this.selectComponent('#commentInput') as any;
          commentInput.reset();
          
          this.setData({ showCommentInput: false });
          this.loadComments();
        } else {
          wx.showToast({ title: res.data.message || '评论失败', icon: 'none' });
          
          const commentInput = this.selectComponent('#commentInput') as any;
          commentInput.setSubmitting(false);
        }
      },
      fail: () => {
        wx.showToast({ title: '网络错误', icon: 'none' });
        
        const commentInput = this.selectComponent('#commentInput') as any;
        commentInput.setSubmitting(false);
      }
    });
  },

  /**
   * 取消评论
   */
  onCommentCancel() {
    this.setData({ showCommentInput: false });
  },

  /**
   * 点击回复
   */
  onReply(e: any) {
    const { comment } = e.detail;
    this.setData({
      showReplyInput: true,
      replyComment: comment
    });
  },

  /**
   * 提交回复
   */
  onReplySubmit(e: any) {
    const { videoId, content, parentId, replyToId } = e.detail;

    wx.request({
      url: 'https://your-api.com/api/interaction/comment',
      method: 'POST',
      data: { videoId, content, parentId, replyToId },
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res: any) => {
        if (res.data.code === 200) {
          wx.showToast({ title: '回复成功', icon: 'success' });
          
          const replyInput = this.selectComponent('#replyInput') as any;
          replyInput.reset();
          
          this.setData({
            showReplyInput: false,
            replyComment: null
          });
          this.loadComments();
        } else {
          wx.showToast({ title: res.data.message || '回复失败', icon: 'none' });
          
          const replyInput = this.selectComponent('#replyInput') as any;
          replyInput.setSubmitting(false);
        }
      },
      fail: () => {
        wx.showToast({ title: '网络错误', icon: 'none' });
        
        const replyInput = this.selectComponent('#replyInput') as any;
        replyInput.setSubmitting(false);
      }
    });
  },

  /**
   * 取消回复
   */
  onReplyCancel() {
    this.setData({
      showReplyInput: false,
      replyComment: null
    });
  }
});
```

---

## 常见问题

### Q1: 如何区分评论和回复？

**A:** 通过 `parentId` 和 `replyToId` 参数区分：

- **发表评论**：不传 `parentId` 和 `replyToId`（或传 `null`）
- **回复评论**：传入 `parentId`（父评论ID）和 `replyToId`（回复目标用户ID）

```typescript
// 发表评论
{ videoId: 1, content: '这个视频真棒！' }

// 回复评论
{ videoId: 1, content: '我也觉得！', parentId: 10, replyToId: 5 }
```

### Q2: 提交成功后如何清空输入框？

**A:** 调用组件的 `reset()` 方法：

```typescript
const commentInput = this.selectComponent('#commentInput') as any;
commentInput.reset();
```

### Q3: 提交失败后如何恢复按钮状态？

**A:** 调用组件的 `setSubmitting(false)` 方法：

```typescript
const commentInput = this.selectComponent('#commentInput') as any;
commentInput.setSubmitting(false);
```

### Q4: 如何实现点击遮罩关闭输入框？

**A:** 组件已内置此功能，用户点击遮罩会自动触发 `cancel` 事件。

### Q5: 如何自定义占位符文本？

**A:** 使用 `placeholder` 属性：

```xml
<comment-input
  placeholder="说点什么..."
  ...
/>
```

### Q6: 回复时如何显示 @用户名？

**A:** 传入 `replyToName` 属性，组件会自动显示：

```xml
<comment-input
  replyToName="{{replyComment.userName}}"
  ...
/>
```

组件会自动显示："回复 @用户名"

### Q7: 如何验证用户是否登录？

**A:** 在显示输入框前检查登录状态：

```typescript
showCommentInput() {
  const token = wx.getStorageSync('token');
  if (!token) {
    wx.showToast({
      title: '请先登录',
      icon: 'none'
    });
    // 跳转到登录页
    wx.navigateTo({
      url: '/pages/auth/login'
    });
    return;
  }
  
  this.setData({ showCommentInput: true });
}
```

### Q8: 如何处理评论审核？

**A:** 后端返回成功后，提示用户"评论成功，等待审核"：

```typescript
wx.showToast({
  title: '评论成功，等待审核',
  icon: 'success',
  duration: 2000
});
```

评论列表只显示审核通过的评论（`status = 1`）。

---

## 总结

### 评论流程
1. 点击"发表评论" → 显示输入框
2. 输入内容 → 点击"发送"
3. 调用API → 提交评论
4. 成功 → 重置输入框 → 刷新列表

### 回复流程
1. 点击评论的"回复" → 显示回复输入框（带@用户名）
2. 输入内容 → 点击"发送"
3. 调用API → 提交回复（包含parentId和replyToId）
4. 成功 → 重置输入框 → 刷新列表

### 关键点
- ✅ 使用两个独立的 comment-input 组件（一个用于评论，一个用于回复）
- ✅ 通过 `show` 属性控制显示/隐藏
- ✅ 通过 `parentId` 和 `replyToId` 区分评论和回复
- ✅ 提交成功后调用 `reset()` 清空输入框
- ✅ 提交失败后调用 `setSubmitting(false)` 恢复状态
- ✅ 刷新评论列表以显示新评论/回复
